@page "/counter"
@using IMS.UseCases.Interfaces

@inject IViewInventoriesByNameUseCase ViewInventoriesByNameUseCase

<PageTitle>Counter</PageTitle>

<h1>Counter</h1>

<p role="status">Current count: @currentCount</p>

<button class="btn btn-primary" @onclick="IncrementCount">Click me</button>
<br/>
<ViewInventoriesComponent OnSearchInventories="OnSearchInventoires"></ViewInventoriesComponent>

@code {
    private int currentCount = 0;

    //testing the use case
    protected override async Task OnInitializedAsync()
    {
        var invs = await ViewInventoriesByNameUseCase.ExcecuteAsync();
    }

    private void IncrementCount()
    {
        currentCount++;
    }
    
    private void OnSearchInventoires(List<Inventory> inventories)
    {

    }
}
